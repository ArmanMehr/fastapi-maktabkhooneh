**NOTE**: I've implemented the redis cache setup in the main app and docker compose but I didn't used caching for my app as there's not any api endpoint having heavy computational cost or external api request or heavy database query.

